from abc import ABC, abstractmethod

class ServerBase(ABC):
    """Abstract base class for Flower servers."""

    @abstractmethod
    def fit(self, int):
        """Run training for certain rounds."""

    @abstractmethod
    def evaluate(self, rnd):
        """Evalute trained model."""
